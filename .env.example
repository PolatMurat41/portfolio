POSTGRES_PRISMA_URL="postgres://user:password@host/db?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://user:password@host/db"
SESSION_SECRET="generate with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
ADMIN_PASSWORD_HASH="generate with: node scripts/hash-password.mjs <your-password> (already base64-encoded, paste as-is)"
BLOB_READ_WRITE_TOKEN="from a Vercel Blob store — Storage tab -> Create Database -> Blob -> Public access"
